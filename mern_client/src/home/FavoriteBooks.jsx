import React, { useEffect, useState} from 'react'

const FavoriteBooks = () => {
  const[books, setBooks] = useState([]);

  useEffect(() => {
    fetch()
  }, [])

  return (
    <div>
      FavoriteBooks
    </div>
  )
}

export default FavoriteBooks
